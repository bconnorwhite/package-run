{
  "name": "package-run",
  "version": "2.0.0",
  "description": "Programmatically run package.json scripts",
  "license": "MIT",
  "author": "Connor White",
  "homepage": "https://connorwhite.org/github/package-run",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bconnorwhite/package-run.git"
  },
  "keywords": [
    "package",
    "pkg",
    "run",
    "yarn",
    "npm",
    "pnpm",
    "manager",
    "npx",
    "bin"
  ],
  "files": [
    "build"
  ],
  "main": "./build/index.js",
  "scripts": {
    "build": "bob build",
    "lint": "bob lint",
    "postversion": "git push",
    "prepublishOnly": "yarn build && yarn test",
    "test": "jest"
  },
  "dependencies": {
    "@bconnorwhite/exec": "^6.0.3",
    "as-typed-array": "^1.0.0",
    "has-script": "^1.0.0",
    "which-pm-lockfile": "^1.0.1"
  },
  "devDependencies": {
    "@bconnorwhite/bob": "^2.3.0",
    "jest": "^26.4.2"
  },
  "eslintConfig": {
    "extends": "eslint-config-bob"
  },
  "npmpackagejsonlint": {
    "extends": "npm-package-json-lint-config-bob"
  }
}
